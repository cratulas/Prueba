<%= form_with(model: booksmark, local: false) do |form| %>
  <% if booksmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booksmark.errors.count, "error") %> prohibited this booksmark from being saved:</h2>

      <ul>
      <% booksmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
